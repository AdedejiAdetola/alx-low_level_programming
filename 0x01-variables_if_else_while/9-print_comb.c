#include <stdio.h>

/**
 * main - entry point
 * Return: 0
 */
int main(void)
{
int j = 48;
int comm = 44;
while (n <= 57)
{
putchar(j);
if (j != 57)
{
putchar(comm);
putchar(32);
}
j += 1;
}
putchar('\n');
return (0);
}
